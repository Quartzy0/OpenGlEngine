package com.quartzy.game;

import com.quartzy.engine.Server;
import lombok.CustomLog;

@CustomLog
public class MainServer{
    
    public static void main(String[] args){
        /*Server server = new Server(1244, "Sandbox");
        server.init(args);
//        World world = WorldLoader.loadWorld(server.getResourceManager().addResource("worlds/100x100_Random.wrld"));
//        World world1 = new World("TestWorld", 10, 10);
//        world1.setTile(0, 0, 0);
//        world1.setTile(1, 1, 1);
//        server.setWorld(world);
        server.run();*/
    }
}
