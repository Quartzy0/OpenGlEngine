package com.quartzy.game;

public class Main{
    public static void main(String[] args){
        /*Client client = new Client("Sandbox");
        client.init(args);
        World world = new World("Tester_man");
        client.setWorld(world);
    
        ECSManager ecsManager = world.getEcsManager();
        short entity = ecsManager.createEntity();
        client.getResourceManager().addResource("textures/tiles/stone.png");
        ecsManager.addComponentToEntity(entity, new TextureComponent(entity, world, client.getTextureManager().getTexture("stone")));
        RigidBody rigidBody = (RigidBody) ecsManager.getComponent(entity, RigidBody.class);
        rigidBody.getBody().translate(200f, 200f);
    
        short entity1 = ecsManager.createEntity();
        ecsManager.addComponentToEntity(entity1, new TextureComponent(entity1, world, client.getTextureManager().getTexture("stone")));
        RigidBody rigidBody1 = (RigidBody) ecsManager.getComponent(entity1, RigidBody.class);
        rigidBody1.getBody().translate(200f, 100f);
        rigidBody1.getBody().setGravityScale(0f);
    
        *//*short entity1 = ecsManager.createEntity();
        ecsManager.addComponentToEntity(entity1, new TextureComponent(entity1, world, client.getTextureManager().getTexture("stone")));
        ecsManager.addComponentToEntity(entity1, new RigidBody(entity1, world, 64, 64, 100, 100));*//*
        
        client.startGame();*/
        
    }
    
}
